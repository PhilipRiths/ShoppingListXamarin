<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ShoppingList.Shared.Views.ShoppingListDetailPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    xmlns:vm="clr-namespace:ShoppingList.Shared.ViewModels;assembly=ShoppingList.Shared"
    Title="Shoppling Lists"
    d:DataContext="{d:DesignInstance vm:ShoppingListDetailViewModel}"
    prism:ViewModelLocator.AutowireViewModel="True"
    mc:Ignorable="d">

    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Entry Placeholder="Shopping list name..." />
        <StackLayout Orientation="Horizontal">
            <Button Command="{Binding CancelCommand}" Text="Cancel" />
            <Button Command="{Binding SaveCommand}" Text="Save" />
        </StackLayout>
    </StackLayout>
</ContentPage>