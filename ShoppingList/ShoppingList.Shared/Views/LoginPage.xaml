<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="ShoppingList.Shared.Views.LoginPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:vm="clr-namespace:ShoppingList.Shared.ViewModels;assembly=ShoppingList.Shared"
             x:Name="ContentPage"
             Title="Login"
             d:DataContext="{d:DesignInstance vm:LoginViewModel}"
             prism:ViewModelLocator.AutowireViewModel="True"
             mc:Ignorable="d">

    <StackLayout Padding="10,40,10,10">
        <Label Text="Sign in"></Label>
        <Image
            Source="btn_google_signin_dark_normal_web.png">
            <Image.GestureRecognizers>
                <TapGestureRecognizer
                    Command="{Binding LoginCommand}" />
            </Image.GestureRecognizers>
        </Image>
    </StackLayout>
    
</ContentPage>